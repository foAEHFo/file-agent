# 实现说明

本实现直接驱动 OpenAI Responses API，没有使用 Agent Runner 框架。客户端回填
模型状态，所有请求使用 `store=false`；reasoning summary 只用于实时展示。CLI
与 Web 共用 Agent、工具、审批和 trace，具有相同的安全边界。

文件系统能力刻意保持有限：没有删除和 shell；路径沙箱拒绝绝对路径、`..` 与
符号链接；覆盖和移动使用读取时的 SHA-256。文件内容均按不可信数据处理，大文件
先逐行搜索再局部读取，并受每 Run 256KB 正文预算约束。

Web 为演示环境设计：共享账号密码、签名 Cookie、单进程内存状态和临时 workspace
副本；Reset 同时恢复文件并清空上下文。写操作逐次审批、批准后立即生效且没有
Run 级回滚；进程重启或闲置一小时会清理会话。

2026-07-31 真实模型验收：T1 命中 10 个文件、按 2025-09 至 2026-01 分组，
局部读取大日志并忽略注入；Reset 恢复文件并清空上下文；T2 经 5 次逐项审批仅
归档三个 obsolete 文件，保留 pricing 文件，MANIFEST 恰好三项。两次有效 Run
各有 15 行严格四字段 trace，T1 无写操作，原始 `workspace/` 未修改。

剩余风险：当前仅在 Python 3.14.6 验证，尚未用目标 Python 3.12 复验；模型服务
曾出现一次上游超时，重试后通过。
