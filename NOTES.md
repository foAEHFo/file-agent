# 实现说明

## 循环与终止条件

CLI/Web 共用一个 ReAct 循环（非使用任何框架）：接收模型输出，校验并执行 function call，再回填
function output。无工具且有回答时完成；无回答或超出 30 次模型、100 次工具、1200
秒时 incomplete；流中断重试一次，仍失败则 failed。

## 上下文里塞什么、不塞什么

我们自己保存上下文，模型上下文只含用户消息、output/reasoning items 和工具结果，不含
整个工作区、trace 或 UI 事件。大文件先搜索再局部读取，正文上限 256KB；文件内容
均不可信，文件名只定位、内容定状态，时间冲突以文件内日期为准。

## 一个关键取舍

写操作逐次审批、批准即生效，简单可审计，但不自动回滚。覆盖和移动要求旧文件或
源文件哈希；不提供删除和 shell，缩小提示词注入的影响面。

## 没做但知道该做的一件事

尚无持久化和多进程恢复，重启会丢失上下文。Reset 只恢复文件并清空上下文，实际reset可以保存上下文；上下文管理机制过于简单，无长期记忆，无摘要或压缩；细致文件操作应凝练为系统skill，目前只用prompt约束；工具审批没有持久化。
